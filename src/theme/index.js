import { createTheme } from "@mui/material";


const Theme = createTheme({
    palette: {
        primary: {
            main: "#47B747"
        },
        secondary: {
            main: "#000000"
        }
    }
});

export default Theme